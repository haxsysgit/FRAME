# minimal-cli fixture

Purpose:
- prove the smallest useful candidate FRAME document can still be meaningful
- ensure omitted parts trigger warnings instead of hard failure in provisional mode

Questions this fixture should pressure-test:
- can facts + rules alone still guide a tiny project?
- what is the minimum acceptable top-level shape?
- how should missing acts/map/expect be reported?
